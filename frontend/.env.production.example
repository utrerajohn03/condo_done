# Copy the value of this into Vercel → your frontend project → Settings →
# Environment Variables (Production). This is NOT read automatically from
# this file — Vite env vars are baked in at build time, so Vercel needs it
# set in the dashboard (or via `vercel env add`) before it builds.

VITE_API_BASE=https://your-backend-project.vercel.app
